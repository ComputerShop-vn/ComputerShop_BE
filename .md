Update: Change EmailService, PaymentScheduleJobService, PenaltyCalculationService to interface, and create class impl in folder service/impl.

And have impl send mail logic, I have config mail in application-local.yml and application-server.yml.
