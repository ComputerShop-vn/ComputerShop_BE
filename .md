Act as an expert Java Spring Boot developer. I need to update the installment payment feature in my project using a standard 3-layer architecture (Controller, Service, Repository/Entity).

Here are the specific business requirements:
1. Users must pay a down payment first. The remaining amount will be divided into installments.
2. In the `InstallmentPackage` entity, add a new field: `downPaymentPercentage` (e.g., representing 10% or 20%).
3. The first installment payment date must be exactly 1 month after the purchase date. 
4. Edge case for date calculation: If the purchase date is on the 31st and the following month has fewer days (e.g., 30 days, or 28/29 days in February), the system must automatically snap to the last day of the following month. (Please use Java's `LocalDate.plusMonths(1)` as it handles this edge case natively).

Please provide the updated code for the following layers:
- Entity Layer: Update the `InstallmentPackage` entity.
- Service Layer: Write a method to calculate the down payment amount, the remaining balance, and generate the schedule of installment due dates based on the logic above.
- Controller Layer: Provide a REST endpoint to calculate and return this installment preview to the user.

Keep the code clean, use best practices, and include comments explaining the date calculation logic.
Note: Update all the related code in the project to support this new feature. And update file init.sql (in docker folder) to add the new field `downPaymentPercentage` to the `installment_package` table. And Update Api Documentation